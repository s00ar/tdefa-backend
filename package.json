{
  "name": "planilleros-backend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node server/index.js",
    "seed:matches": "node server/seed.js",
    "test": "node --test tests/unit/*.js && node --test tests/integration/*.js && npm run test:smoke",
    "test:smoke": "node --test tests/smoke/*.js"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "mysql2": "^3.22.5"
  },
  "devDependencies": {
    "@types/node": "^24.12.2"
  }
}
